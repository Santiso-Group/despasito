name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - deap
  - matplotlib
  - numba

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


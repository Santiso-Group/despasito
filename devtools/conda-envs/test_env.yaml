name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - jax
  - jaxlib
  - numba

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


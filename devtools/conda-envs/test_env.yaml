name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - numba

    # Testing
  - pytest
  - pytest-cov
  - codecov

   # Documentation
  - sphinx
  - sphinx_rtd_theme
  - sphinx-argparse

    # Pip-only installs
  #- pip:
  #  - codecov

